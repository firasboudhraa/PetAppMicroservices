eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

server:
  port: 8081

spring:
  application:
    name: user_service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_DB?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

rabbitmq:
  exchanges:
    petservice: petservice.exchange
  queues:
    petservice: petservice.queue
  routing-keys:
    petservice: petservice.routing-key

application:
  config:
    appointments-url: http://localhost:8091/api/appointments



management:
  tracing:
    sampling:
      probability: 1.0