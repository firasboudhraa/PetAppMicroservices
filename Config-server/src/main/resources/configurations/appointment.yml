eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

server:
  port: 8091

spring:
  application:
    name: appointment
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/appointments?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: fboudhraa2@gmail.com
    password: abbt fxed mcsf fryl
    properties:
      mail.smtp:
        auth: true
        starttls:
          enable: true
          required: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

rabbitmq:
  exchanges:
    petservice: petservice.exchange
    appointment: appointment.exchange
  queues:
    petservice: petservice.queue
  routing-keys:
    petservice: petservice.routingkey


application:
  config:
    notifications-url: http://localhost:8055

management:
  tracing:
    sampling:
      probability: 1.0