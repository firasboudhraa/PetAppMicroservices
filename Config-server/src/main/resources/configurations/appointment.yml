eureka:
  instance:
    hostname: ${EUREKA_SERVER_HOSTNAME:localhost}
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

server:
  port: 8091

spring:
  application:
    name: appointment
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:3306/appointments?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: fureverbuddy93@gmail.com
    password: mwgizughkyacrmaz
    properties:
      mail.smtp:
        auth: true
        starttls:
          enable: true
          required: true

rabbitmq:
  exchanges:
    petservice: petservice.exchange
    appointment: appointment.exchange
  queues:
    petservice: petservice.queue
  routing-keys:
    petservice: petservice.routingkey


application:
  config:
    notifications-url: http://${NOTIFICATIONS8URL:localhost}:8055
    user-url: http://${USER_URL:localhost}:8084
