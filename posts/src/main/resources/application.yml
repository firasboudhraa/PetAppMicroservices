spring:
  application:
    name: posts
  config:
    import: optional:configserver:http://localhost:8888
  main:
    allow-bean-definition-overriding: true

  # Email Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: fureverbuddy93@gmail.com
    password: abxm iszx fsse pqyf


    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
