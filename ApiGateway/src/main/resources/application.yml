spring:
  application:
    name: gateway
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost}:8888

level:
   org:
     springframework:
       cloud:
         gateway: DEBUG