spring:
  application:
    name: MedicalNotebook
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost}:8888
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
