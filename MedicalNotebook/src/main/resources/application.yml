spring:
  application:
    name: MedicalNotebook
  config:
    import: "optional:configserver:http://localhost:8888"
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB



application:
  config:
    medicalrecords-url: "http://localhost:8071/medical-records"
